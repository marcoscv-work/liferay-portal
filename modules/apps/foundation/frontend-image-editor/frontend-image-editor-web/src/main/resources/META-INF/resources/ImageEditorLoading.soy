{namespace ImageEditorLoading}

/**
 * Image Editor Loading Component
 *
 * @param? show
 */
{template .render}
	<span class="{$show ? '' : 'fade'} loading-icon loading-icon-dotted">
		<span class="loading-icon-indicator"></span>
	</span>
{/template}